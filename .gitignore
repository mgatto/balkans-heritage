.idea
.vscode
node_modules
/package-lock.json
/dist
.wrangler
.lighthouseci
coverage/
*.report.html
.DS_Store

# BrowserStack real-browser screenshots (manual `bs:screenshots` runs)
screenshots/
local.log
.env

# External-link checker redirect log (manual `check:links` runs)
link-check-redirects.log

# Local-only notes never meant to be committed to this public repo
/private/
